using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrototypeDebug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("nature is alive!");
        Debug.LogWarning("so care about the nature!");
        Debug.LogError("don't the destroy nature!");

    }

    // Update is called once per frame
    void Update()
    {
       Debug.Log("spaaaaaaaaaaaaaaaaaaam!"); 
    }
}
